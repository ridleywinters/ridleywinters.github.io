# Sea Test Page

<div>
    {(() => {
        const parts = [];
        page.visit( (node) => {
            if (node.type === 'heading' && node.depth === 2) {
                parts.push(node);
            }
        })
        return (
            <div>
                <strong>Table of Contents</strong>
                {parts.map((node) => <div><Sea tree={node.children} /></div> )}
            </div>
        );
    })()}
</div>

## Heading 2: A

## Heading 2: B


```@expand
title: test
content$sea: Hi
```

## Heading 2: C

## Heading 2: D