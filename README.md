# My Homepage

A single-page web application using unified, remark, MDX, and React among other technologies.

## TODO 

- [ ] Add custom JSX elements
- [ ] Add hot-reloading (webpack instead of parcel)
- [ ] Remove .babelrc dependency (zero-config?)
- [x] Add `make dev` (zero-config)

